import React from 'react';
import PropTypes from 'prop-types';

const Cousin = props => {
    return (
        <div>
            <h4>Cousin</h4>
        </div>
    );
};

Cousin.propTypes = {
    
};

export default Cousin;